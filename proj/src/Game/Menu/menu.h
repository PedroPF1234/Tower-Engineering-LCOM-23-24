#pragma once

#include "../../Devices/device_controller.h"

/**
 * @brief Represents the Main Menu.
 */

/**
 * @brief Initializes the menu.
 */
void initializeMenu();

/**
 * @brief Enters the menu.
 */
void enterMenu();

/**
 * @brief Updates the menu.
 */
void updateMenu();

/**
 * @brief Exits the menu.
 */
void exitMenu();

/**
 * @brief Destroys the menu's associated GameObjects and Buttons, freeing their memory.
 */
void destroyMenu();

